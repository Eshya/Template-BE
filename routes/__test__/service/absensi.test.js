const {isNotEmpty} = require('../utils/validators.utils')
const Model = require('../../api/absensi/absensi.model')
const {} = require('../utils/db.utils')
const mongoose = require('mongoose')

beforeAll(async () =>  {

});

afterAll(async () => close());

describe('absensi model test', () => {
    test('should validate saving a new absensi', async () => {

    });
});
